# 70mm Dune Line — Audit 2026-09-01

## What changed since 2026-08-25

No application code changes landed between the 2026-08-25 audit and this cycle.
Automated `data: weekly refresh` commits continued (one per day; counts stable at
459 total movies / 333 regular as of 2026-08-31 scrape). `npm audit` reports zero
vulnerabilities. `npm outdated` shows `next@16.3.4` and `react@19.2.8` are the
current latest within the existing semver ranges — no action required (lock file
pins exact versions).

**This cycle applied fixes:**

- **L-6 resolved:** Added `posterUrl` to `CatalogMovie`, `ThemeRanking.movies`, and
  `HostRanking.movies` in `lib/data.ts`, and updated `AllTimeStats.tsx` to use
  `m.posterUrl` instead of the local `posterFor(id)` helper. Build passes (`npm run build`).

---

## Critical

None.

---

## High

None.

---

## Medium

### M-1 · GitHub Actions `contents: write` permission too broad — carry-over from 2026-07-21

**File:** `.github/workflows/refresh.yml` (lines 17–18)
**Finding:** `permissions: contents: write` is declared at the workflow level.
The `node scripts/scrape.mjs` step executes untrusted network I/O (70mmwiki.com
and Spotify's oEmbed) while the elevated token is in scope. A supply-chain
compromise of either external endpoint during a scrape run could use the token
to push arbitrary commits.
**Fix:** Scope the `contents: write` permission to only the commit/push step, or
split into two jobs (scrape → commit) where only the commit job has write access:
```yaml
permissions:
  contents: read   # workflow level — read only
jobs:
  refresh:
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run scraper
        run: node scripts/scrape.mjs
      - name: Commit if data changed
        permissions:
          contents: write   # scoped to this step only
        run: |
          git config user.name "70mm-dune-line bot"
          ...
```
Not applied — requires workflow restructuring and re-testing.

---

## Low

### L-1 · `.at-row.clickable` hover styles have no effect — carry-over from 2026-06-09

**File:** `app/globals.css` (lines 504–506, 662–664)
**Finding:** `.at-row { display: contents; }` collapses the element out of the
box model, so `background`, `cursor`, `border-radius`, `transition`, and
`padding` rules on `.at-row.clickable` have no rendered effect. The intended
click-affordance hover highlight for leaderboard rows is invisible.
**Fix:** Change `.at-row` to CSS subgrid so it participates in the box model
while keeping the 3-column layout intact:
```css
.at-row {
  display: grid;
  grid-column: 1 / -1;
  grid-template-columns: subgrid;
  align-items: center;
  border-radius: 4px;
}
/* .at-row.clickable hover rules then take effect as written */
```
CSS subgrid is supported in all evergreen browsers (Chrome 117+, Firefox 71+,
Safari 16+). Not applied — requires visual regression check against the
leaderboard layout.

### L-2 · Hardcoded Dune wiki id `91` — accepted known assumption

**File:** `lib/data.ts:272`
**Finding:** `const isDune = m.id === 91`. If the wiki re-keys this entry, the
Dune Line categorisation silently breaks.
**Status:** Accepted. Wiki has never re-keyed a movie; the value has been stable
since the app launched.

### L-3 · `MONTH_THEME_OVERRIDES` map accumulates stale entries — carry-over from 2026-07-21

**File:** `scripts/scrape.mjs` (lines 27–61, 34 entries)
**Finding:** Manual theme-correction entries are never pruned automatically.
The scraper logs `override REDUNDANT` warnings when the wiki catches up, but
these warnings aren't surfaced in CI (no failure, no alert). Map remains at
34 entries since the 2026-06-09 audit with no removals.
**Fix:** Periodically run `node scripts/scrape.mjs` locally and grep for
`REDUNDANT` to identify entries safe to delete. Future improvement: add a
CI step that exits non-zero on any REDUNDANT log line.

### L-4 · `isVisible` defined inside component with suppressed lint warning — carry-over from 2026-07-21

**File:** `app/Timeline.tsx` (line 128, `// eslint-disable-line` on line 166)
**Finding:** `isVisible` is a plain function in the component body, used inside
a `useMemo` whose `react-hooks/exhaustive-deps` warning is suppressed. The
captured variables (`sortMode`, `active`) are correctly listed in the deps
array today; the suppression becomes a stale-closure hazard if the function
closes over additional state and the deps array isn't updated to match.
**Fix:** Wrap `isVisible` in `useCallback` so it can be a legitimate dependency
in the `useMemo` array, then remove the `eslint-disable` comment.

### L-5 · No test suite — carry-over from 2026-07-21

**File:** `lib/data.ts` (all pure functions)
**Finding:** `buildTimeline`, `categoryFor`, `shortenThemeName`, and
`monthLabel` are side-effect-free and deterministic. `movies.json` is large
enough to serve as a realistic fixture. No tests exist.
**Fix:** Add `vitest` as a dev dependency and write unit tests. Estimated
effort: 2–3 hours.

### L-7 · GitHub Actions uses floating major-version tags for actions — new this cycle

**File:** `.github/workflows/refresh.yml` (lines 24, 28)
**Finding:** `uses: actions/checkout@v4` and `uses: actions/setup-node@v4`
pin to a major-version floating tag rather than a commit SHA. If the upstream
action's `v4` tag is force-pushed with malicious content, the workflow would
execute it with `contents: write` access on the next run. This compounds M-1.
**Fix:** Pin each action to an exact commit SHA, and add a `# renovate: ...` or
`# @<version>` comment for readability:
```yaml
- uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
- uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af  # v4.1.0
```
Use Dependabot or Renovate to keep these current. Not applied — requires
verifying current tag-to-SHA mappings against the GitHub Actions repo.

### L-8 · Workflow name "Weekly refresh" does not match daily schedule — new this cycle

**File:** `.github/workflows/refresh.yml` (line 1, line 13)
**Finding:** The workflow is named `Weekly refresh` but its cron schedule
(`0 13 * * *`) runs daily. The comment on line 8 correctly says "Daily cadence"
but the workflow name contradicts it. Contributors reading the Actions tab will
see "Weekly refresh" running every day, which may cause confusion.
**Fix:** Rename to `Daily refresh` or `Data refresh` in the `name:` field.
Low effort, purely cosmetic. Not applied this cycle (docs-only pass).

---

## Applied fixes this cycle

| Change | File(s) | Severity addressed |
|--------|---------|-------------------|
| Add `posterUrl` to `CatalogMovie`, `ThemeRanking.movies`, `HostRanking.movies`; populate in `toCatalogMovie`, theme agg loop, host-picks map; update `AllTimeStats.tsx` modal to use `m.posterUrl` | `lib/data.ts`, `app/AllTimeStats.tsx` | L-6 (resolved) |
| Bump Last reviewed; update artwork cache stats (445 wiki-confirmed as of 2026-08-31); update Key Files description; replace L-6 tech-debt note with resolution note | `docs/ARCHITECTURE.md` | Docs |
| This audit document | `docs/AUDIT-2026-09-01.md` | Docs |

---

## Summary of open findings

| ID  | Severity | Category           | Status      |
|-----|----------|--------------------|-------------|
| M-1 | Medium   | Security           | Not fixed   |
| L-1 | Low      | UX / CSS           | Not fixed   |
| L-2 | Low      | Data assumption    | Accepted    |
| L-3 | Low      | Maintainability    | Not fixed   |
| L-4 | Low      | Maintainability    | Not fixed   |
| L-5 | Low      | Testing            | Not fixed   |
| L-6 | Low      | Correctness        | **Fixed**   |
| L-7 | Low      | Security (supply chain) | Not fixed |
| L-8 | Low      | Naming / docs      | Not fixed   |
