# 70mm Dune Line — Audit 2026-08-25

## What changed since 2026-08-18

No application code changes since the 2026-08-18 cycle. Only automated
`data: weekly refresh` commits landed between 2026-08-18 and this cycle
(one new episode added: total 459 movies, 333 regular as of 2026-08-24).
`npm audit` continues to report zero vulnerabilities.

**This cycle applied fixes:** docs only — ARCHITECTURE.md updated to reflect
current data counts and add L-6 (AllTimeStats poster URL inconsistency).

---

## Critical

None.

---

## High

None.

---

## Medium

### M-1 · GitHub Actions `contents: write` permission too broad — carry-over from 2026-07-21

**File:** `.github/workflows/refresh.yml` (lines 17–18)
**Finding:** `permissions: contents: write` is declared at the workflow level.
The `node scripts/scrape.mjs` step executes untrusted network I/O (70mmwiki.com
and Spotify's oEmbed) while the elevated token is in scope. A supply-chain
compromise of either external endpoint during a scrape run could use the token
to push arbitrary commits.
**Fix:** Set `permissions: contents: read` at the workflow level and override
with `contents: write` only in the specific step that does the `git push` —
or split into two jobs (scrape → commit) where only the commit job has write
permission.

---

## Low

### L-1 · `.at-row.clickable` hover styles have no effect — carry-over from 2026-06-09

**File:** `app/globals.css` (lines 504–506, 662–664)
**Finding:** `.at-row { display: contents; }` collapses the element out of the
box model, so `background`, `cursor`, `border-radius`, and `transition` rules
on `.at-row.clickable` have no rendered effect. The intended click-affordance
hover highlight for leaderboard rows is invisible to users.
**Fix:** Change `.at-row` to use CSS subgrid so it participates in the box
model while keeping the 3-column layout intact:
```css
/* .all-time-grid stays as-is */
.at-row {
  display: grid;
  grid-column: 1 / -1;
  grid-template-columns: subgrid;
  align-items: center;
  border-radius: 4px;
}
/* .at-row.clickable hover rules then take effect as written */
```
CSS subgrid is supported in all evergreen browsers (Chrome 117+, Firefox 71+,
Safari 16+). Not applied — requires visual regression check against the
leaderboard layout.

### L-2 · Hardcoded Dune wiki id `91` — accepted known assumption

**File:** `lib/data.ts:272`
**Finding:** `const isDune = m.id === 91`. If the wiki re-keys this entry, the
Dune Line categorisation silently breaks.
**Status:** Accepted. Wiki has never re-keyed a movie; the value is stable.

### L-3 · `MONTH_THEME_OVERRIDES` map accumulates stale entries — carry-over from 2026-07-21

**File:** `scripts/scrape.mjs` (lines 27–61, 34 entries)
**Finding:** Manual theme-correction entries are never pruned automatically.
The scraper logs `override REDUNDANT` warnings when the wiki catches up, but
these warnings aren't surfaced in CI (no failure, no alert). Map remains at
34 entries since the 2026-06-09 audit.
**Fix:** Periodically run `node scripts/scrape.mjs` locally and grep for
`REDUNDANT` to identify entries safe to delete. Future improvement: add a
CI step that fails (or at least warns loudly) on any REDUNDANT log line.

### L-4 · `isVisible` defined inside component with suppressed lint warning — carry-over from 2026-07-21

**File:** `app/Timeline.tsx` (line 128, `// eslint-disable-line` on line 166)
**Finding:** `isVisible` is a plain function inside the component body, used
inside a `useMemo` whose `react-hooks/exhaustive-deps` warning is suppressed.
The captured variables (`sortMode`, `active`) are present in the deps array, so
the suppression is safe today. It becomes a stale-closure hazard if the function
ever closes over additional state without the deps array being updated.
**Fix:** Wrap `isVisible` in `useCallback` so it can be listed as a legitimate
dependency in the `useMemo` array, then remove the `eslint-disable` comment.

### L-5 · No test suite — carry-over from 2026-07-21

**File:** `lib/data.ts` (all pure functions)
**Finding:** `buildTimeline`, `categoryFor`, `shortenThemeName`, and
`monthLabel` are side-effect-free and deterministic. They have no tests.
`movies.json` is large enough to serve as a realistic fixture.
**Fix:** Add `vitest` as a dev dependency and write unit tests. Estimated
effort: 2–3 hours.

### L-6 · AllTimeStats modal poster URL ignores `spotifyThumb` fallback — new this cycle

**File:** `app/AllTimeStats.tsx:40`
**Finding:** The `posterFor` helper always constructs the wiki artwork URL
(`https://70mmwiki.com/api/artwork/thumbs/{id}.jpg`) from the movie id alone.
It does not consult `spotifyThumb`. This is inconsistent with `lib/data.ts`
which builds `DisplayMovie.posterUrl` as `spotifyThumb || wiki-url`. Any movie
whose wiki artwork 404s and whose poster was captured via Spotify oEmbed will
display a broken image in the leaderboard modal, while the same movie's bar in
the timeline renders the Spotify image correctly.
**Current impact:** Zero — as of 2026-08-24 no regular episodes use the Spotify
fallback (all 331 wiki-confirmed). Two newly-published unrated episodes (ids 457
and 458) have no art at all via either path, which is expected for fresh entries.
**Fix:** Add `posterUrl: string` to the `CatalogMovie` interface and populate it
in `toCatalogMovie` in `lib/data.ts`:
```ts
// in toCatalogMovie (lib/data.ts ~line 414)
const toCatalogMovie = (m: RawMovie): CatalogMovie => ({
  // ... existing fields ...
  posterUrl: m.spotifyThumb || `https://70mmwiki.com/api/artwork/thumbs/${m.id}.jpg`,
});
```
Then replace `posterFor(m.id)` in `AllTimeStats.tsx:281` with `m.posterUrl`.
Not applied this cycle — zero current impact, low urgency.

---

## Applied fixes this cycle

| Change | File | Severity addressed |
|--------|------|--------------------|
| Bump Last reviewed date; update data counts (459 total / 333 regular); add L-6 to tech debt; clarify RATING_OVERRIDES empty, artwork cache stats | `docs/ARCHITECTURE.md` | Docs |
| This audit document | `docs/AUDIT-2026-08-25.md` | Docs |

---

## Summary of open findings

| ID  | Severity | Category        | Status    |
|-----|----------|-----------------|-----------|
| M-1 | Medium   | Security        | Not fixed |
| L-1 | Low      | UX / CSS        | Not fixed |
| L-2 | Low      | Data assumption | Accepted  |
| L-3 | Low      | Maintainability | Not fixed |
| L-4 | Low      | Maintainability | Not fixed |
| L-5 | Low      | Testing         | Not fixed |
| L-6 | Low      | Correctness     | Not fixed |
